public class test {
    if (screenShot.getRGB(x, y) == refPic.getRGB(0, 0)) {
        count = 1;
        int y1 = 0;
        int x1 = 1;
        while (y1 < refPic.getHeight() && x1 < refPic.getWidth() - 1
                && screenShot.getRGB(x + x1, y + y1) == refPic.getRGB(x1, y1)) {
            x1++;
            if (x1 > refPic.getWidth()) {
                x1 = 0;
                y1++;
            }
            count++;
            if (count == (refPic.getHeight() * refPic.getWidth()) - 100) {
                cords[0] = x;
                cords[1] = y;
                cords[2] = 1;
                return cords;
            }

        }
    } else {
        count = 0;
    }
}
